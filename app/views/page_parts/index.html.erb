<div class="panel panel-default"> 
    <div class="panel-heading"> 
      <p id="notice">
        <%= notice %></p>


<%= params["contract_id"] %>
</div>
<%= # render "sort/mycontracts" 
%>

    <div class="panel-heading">
    <h1>Page Sections B</h1>

    </div>

    <div class="panel-body"> 

      <table>
        <thead>
          <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Content</th>
            <th>Contract</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @page_parts.each do |page_part| %>
            <tr>
              <td><%= page_part.id %></td>
              <td><%= page_part.name %></td>
              <td><%= page_part.content %></td>
              <td><%= page_part.contract_id %></td>
              <td><div class="btn btn-default"><%= link_to 'Show', page_part %></div></td>
              <td><div class="btn btn-info"><%= link_to 'Edit', edit_page_part_path(page_part) %></div></td>
              <td><div class="btn btn-danger"><%= link_to 'Destroy', page_part, method: :delete, data: { confirm: 'Are you sure?' } %></div></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
<br>


<% if(params[:contract_id] != nil) %>
<div class="btn btn-info"><%= link_to "New Section in Contract #{@contract.id}", new_page_part_path(contract_id: params[:contract_id]) %></div>

<div class="btn btn-info"><%= link_to "New Layout in Contract #{@contract.id}", new_page_layout_path(contract_id: params[:contract_id]) %></div>
<%= # link_to 'New Page Part', new_page_part_path(contract_id: params[:id]) 
 %>
 <% else %>
 <div class="btn btn-info"><%= #link_to "New Section in Contract #{@contract.id}", new_page_part_path(contract_id: params[:contract_id]) 
 %></div>

<div class="btn btn-info"><%= #link_to "New Layout in Contract #{@contract.id}", new_page_layout_path(contract_id: params[:contract_id]) 
%></div>
<% end %>
<%= # render "sort/layouts" 
    %>

</div>
<%=  render "sort/menu" 
%>